# NOVA Frontend Environment Configuration

# Optional: URL of your deployed FastAPI backend (e.g., on Render, Railway, AWS, or an ngrok tunnel)
# If omitted, defaults to http://127.0.0.1:8000 for local development.
BACKEND_URL=http://127.0.0.1:8000
